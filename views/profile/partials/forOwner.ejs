<nav class="col-lg-6 navbar navbar-expand-lg navbar-dark bg-dark" style="margin-left: 25%;">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup" style="text-align: center;">
      <div class="navbar-nav">
        <%if(typeof page == "undefined"){%>
  
        <% } else { %>
        <a class="nav-item nav-link <% if(page === 'userProfile'){ %> active <% } else {%><% } %>" href="/users/<%= foundUser._id %>">Home</a>
        <a class="nav-item nav-link <% if(page === 'about') { %>active<% } %>" href="/users/<%= foundUser._id %>/about">About</a>
        <a class="nav-item nav-link <% if(page === 'updateProfile') { %>active<% } %>" href="/users/<%= foundUser._id %>/update-profile">Update Profile</a>
        <a class="nav-item nav-link <% if(page === 'updateProfilePhoto') { %>active<% } %>" href="/users/<%= foundUser._id %>/update-profile-photo">Change Profile Photo</a>
        <% } %>
      </div>
    </div>
  </nav>
  <br>
  <br>